<h4>Search</h4>
    <%= form_tag(movies_path, method: :get) do %>
        <%= text_field_tag(:search, params[:search]) %>
        <%= submit_tag("Search") %>
    <% end %>
<br>
<small id="notice"><%= notice %></small>
<%= render 'list', movies: @movies %>
